<%= form_with(model: raffle, local: true) do |form| %>
  <% if raffle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(raffle.errors.count, "error") %> prohibited this raffle from being saved:</h2>

      <ul>
      <% raffle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :drawing_date %>
    <%= form.text_field :drawing_date, id: :raffle_drawing_date %>
  </div>

  <div class="field">
    <%= form.label :prize_amount %>
    <%= form.number_field :prize_amount, id: :raffle_prize_amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
